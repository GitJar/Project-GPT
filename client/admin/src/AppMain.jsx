/* AppMain.jsx - simplified admin UI (use the full component exported earlier in design) */
import React from 'react';
import AdminApp from './App'; // the large App exported earlier
export default function App() {
  return <AdminApp />;
}
